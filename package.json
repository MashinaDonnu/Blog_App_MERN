{
  "name": "blog_react",
  "version": "1.0.0",
  "description": "blog from react",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=production node index.js",
    "client:install": "npm install --prefix client",
    "client:build": "npm run build --prefix client",
    "server": "npx nodemon",
    "client": "npm start --prefix client",
    "dev": "cross-env NODE_ENV=development concurrently \"npm run server\" \"npm run client\""
  },
  "keywords": [
    "js",
    "react",
    "nodejs"
  ],
  "author": "Dmitriy Zahatin <zagatin999@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "concurrently": "^5.3.0",
    "config": "^3.3.1",
    "cross-env": "^7.0.2",
    "cryptr": "^6.0.2",
    "express": "^4.17.1",
    "express-validator": "^6.6.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.10.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
